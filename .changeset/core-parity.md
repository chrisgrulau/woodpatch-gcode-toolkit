---
'@woodpatch/gcode-core': patch
---

No API change. The parity ledger (`tools/parity.mjs`, `docs/PARITY.md`) shows that
every difference between the core's path and upstream's, across all fixtures,
comes from a known upstream defect or a recorded decision. CI checks it.
