// SPDX-FileCopyrightText: 2026 Promotional Notions Pty Ltd trading as Woodpatch House & Garden
// SPDX-License-Identifier: MIT
//
// The playground's worker is just the viewer's worker entry. A bare import is enough:
// the package declares that entry as having side effects, so the bundler keeps it.
import '@woodpatch/gcode-viewer/worker';
