{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Source package: nothing is emitted (ADR-0030). svelte-check reads this.
    "noEmit": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node"],
    "allowJs": true,
    "checkJs": true
  },
  "include": ["src", "test", "vitest.config.ts"]
}
